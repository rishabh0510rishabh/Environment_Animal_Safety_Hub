class BlogLikeManager{constructor(){this.init();}init(){this.likeButtons=document.querySelectorAll('.like-button');this.likeButtons.forEach(button=>{this.setupButton(button);});}setupButton(button){const blogId=this.getBlogId();const storageKey=`blog_like_${blogId}`;const counter=button.querySelector('.like-counter');// Load initial count from localStorage let currentCount=parseInt(localStorage.getItem(storageKey))|| 0;counter.textContent=currentCount;// Check if user already liked this blog const hasLiked=localStorage.getItem(`blog_liked_${blogId}`)==='true';if(hasLiked){button.classList.add('liked');}button.addEventListener('click',()=>{if(!hasLiked){currentCount++;counter.textContent=currentCount;localStorage.setItem(storageKey,currentCount);localStorage.setItem(`blog_liked_${blogId}`,'true');button.classList.add('liked');// Add animation button.style.transform='scale(1.1)';setTimeout(()=>{button.style.transform='scale(1)';},200);}});}getBlogId(){// Use the page title or URL path as unique identifier const title=document.title.split(' | ')[0].toLowerCase().replace(/\s+/g,'-');return title;}}// Initialize when DOM is loaded document.addEventListener('DOMContentLoaded',()=>{new BlogLikeManager();});