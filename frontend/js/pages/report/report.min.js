//==========AOS INITIALIZATION==========AOS.init({duration: 1000,// Animation duration in milliseconds once: true,// Animation occurs only once per element});//==========FORM HANDLING==========const reportForm=document.getElementById("reportForm");reportForm.addEventListener("submit",function(e){// Prevent default form submission e.preventDefault();// Show success message with emoji alert("🦊 Thank you! Your report has been submitted successfully 💚");// Reset form to clear all fields this.reset();});