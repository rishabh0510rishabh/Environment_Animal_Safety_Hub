//==========DOM ELEMENTS==========const form=document.getElementById("reportForm");const successMsg=document.getElementById("successMsg");//==========FORM HANDLING==========form.addEventListener("submit",function(e){// Prevent default form submission e.preventDefault();// Display success message successMsg.style.display="block";// Reset form to clear all input fields form.reset();// Smooth scroll to success message with offset for better visibility window.scrollTo({top: successMsg.offsetTop-100,behavior: "smooth"});});